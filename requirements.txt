# =============================================================================
# Claims ChatGPT - Requirements
# Install with: pip install -r requirements.txt
# =============================================================================

# --- Core Data ---
pandas>=1.5.0             # 1.5+ works on Python 3.7, 3.8, 3.9, 3.10, 3.11, 3.12
numpy>=1.21.0
pyqvd>=0.3.0              # QVD file reader (only needed when using real QVDs)
pyyaml>=5.4.0             # Config file parsing

# --- AI & Embeddings (Phase 2) ---
sentence-transformers>=2.2.0   # Text → embeddings (CPU friendly)
faiss-cpu>=1.7.0               # In-memory vector search
ollama>=0.1.0                  # Ollama Python client for Llama3
langchain>=0.1.0               # LLM orchestration helpers
langchain-community>=0.0.20    # LangChain community integrations

# --- Streamlit UI (Phase 3) ---
streamlit>=1.20.0

# --- Teams Bot (Phase 3 - not needed for Streamlit-only) ---
botbuilder-core>=4.14.0
botbuilder-integration-aiohttp>=4.14.0

# --- Notifications (Phase 4) ---
pymsteams>=0.2.2          # Teams webhook
# smtplib is built into Python - no install needed

# --- Scheduling ---
schedule>=1.1.0

# --- Dev / Testing ---
pytest>=7.0.0
