# assets/

Drop your corporate PowerPoint template here as `default_template.pptx`. The deck
builder uses its Slide Masters for branding and extracts brand colours from the
theme. If no template is present, python-pptx's built-in default is used and charts
fall back to the neutral brand palette in `render.py`.
